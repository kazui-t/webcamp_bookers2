
<div class="col-md-3">
  <div class="profile-container">
    <h2>User info</h2>
    <%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
    <div class="row">
        <h4 class="col">Name</h4>
        <p class="col"><%= user.name %></p>
    </div>
    <div class="row">
        <h4 class="col">Introduction</h4>
        <p class="col"><%= user.introduction %></p>
    </div>

    <!--<%# if user.id == current_user.id %>-->
    <!--    <div class="btn btn-outline-secondary btn-block">-->
    <!--        <%#= link_to edit_user_path(current_user) do %><i class="fas fa-user-cog"></i><%# end %>-->
    <!--    </div>-->
    <!--<%# else %>-->
    <!--    <div class="btn btn-outline-secondary btn-block">-->
    <!--      <%#= link_to user_path(current_user) do %><i class="fas fa-user-cog"></i><%# end %>-->
    <!--    </div>-->
    <!--<%# end %>-->

<%= link_to '', edit_user_path(user.id), class: "btn btn-outline-secondary btn-block fas fa-user-cog" %>
  </div>


  　　<h1>New book</h1>
      <%= form_with model: book, local:true do |f| %>
       <h4>Title</h4>
      <%= f.text_field :title %>
       <h4>Opinion</h4>
       <%= f.text_field :body %>
       <%= f.submit 'Create Book' , class: "btn btn-secondary"%>
      <% end %>
</div>
